Run the consistency validation script:
```bash
pnpm run validate
```
This checks: save fields vs migration columns, room exit integrity, NPC topic coverage.
Report any failures with specific files and fields.
